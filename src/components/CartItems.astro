---
interface Props {
	
	id: string
	image: string
	title: string
	consoles: string[]
	price: number
	quantity: number
}
const { id, image, title, consoles, price, quantity } = Astro.props;
---
<div class="flex bg-[#cecece] rounded-lg mt-4 items-center">
    <div class="h-64 px-8 overflow-hidden items-center flex">
        <div class="h-40 overflow-hidden rounded-lg">
            <img 
                src={image}
                class="w-40 object-cover  items-center flex"
                alt={title}
            >
        </div>
    
        <div class="ml-4">
            <h2>{title}</h2>
            <p>{consoles.join(', ')}</p>
            <h3>€ {price}</h3>
        </div>
    
    
    
    </div>
    <div class="flex pr-8 text-right">
        <h3 class="items-center flex">x{quantity}</h3>
    </div>
</div>
